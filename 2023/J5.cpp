#include <bits/stdc++.h>

int searcher(char letter, std::string word){
    if ()
}

int main(){
    int R,C;
    std::string word, row;
    std::cin>>word>>R>>C;
    std::vector<std::vector<char>>grid;
    for (int i = 0; i<C; i++){
        std::cin>> row;
        std::vector<std::vector<char>>grid(row.begin(), row.end());
    }
}